import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#550016",
    },
    secondary: {
      main: "#1C0076",
    },
  },
});

export default theme;
